// Central UI timing, spacing and visual density configuration
const UI = {
  // animation durations (seconds) - slightly slower for smooth, premium transitions
  ANIM_FAST: 0.5,
  ANIM_MED: 0.8,
  ANIM_SLOW: 1.0,

  // message rotation (ms)
  MESSAGE_ROTATION_INTERVAL: 5000,

  // Explore Mode timings (ms)
  EXPLORE_STEP_DELAY: 5600,
  HIGHLIGHT_DURATION: 3500,

  // 3D scene density & speeds
  STAR_COUNT_DEFAULT: 480,
  STAR_ROTATION_SPEED: 0.008,
  STAR_POINT_SIZE: 0.5,
  STAR_OPACITY: 0.6,

  PARTICLES_COUNT_DEFAULT: 28,
  PARTICLES_EMISSIVE_INTENSITY: 0.36,

  PROJECT_NODES_BASE_SPEED: 0.12,
  GROUP_ROTATION_SPEED: 0.012,

  CUBE_ROT_X_MULT: 0.3,
  CUBE_ROT_Y_MULT: 0.18,

  EGG_EMISSIVE_INTENSITY: 0.12,
}

export default UI
